# basenc

> Personal quick reference on basenc.

- Base64 encode:

`basenc --base64 -w 0`

- Base64 decode:

`basenc --base64 --decode`

- Base64url encode:

`basenc --base64url -w 0 | tr -d '='`

- Base64url decode:

`basenc --base64url --decode`
