- Get "friendly" name:

`scutil --get ComputerName`

- Get the name of the Bonjour / mDNS / ZeroConf host name:

`scutil --get LocalHostName`

- Get whatever the command `hostname` returns:

`scutil --get HostName`
