;;; init-search.el --- init-search.el -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(setq
 ;; Show the order of the match and the total number of matches in the echo area.
 isearch-lazy-count t
 ;; Never wrap
 isearch-wrap-pause nil)

(provide 'init-search)
;;; init-search.el ends here
